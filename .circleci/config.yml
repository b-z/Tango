version: 2.1
jobs:
  build-nodejs:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          command: cd legacy-nodejs
      - run:
          command: make build

  build-python:
    docker:
      - image: circleci/python:latest
    steps:
      - ckeckout
      - run:
          command: cd python
      - run:
          command: make deps
workflows:
  tango:
    jobs:
      - build-nodejs
      - build-python
